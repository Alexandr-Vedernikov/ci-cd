---
all:
  hosts:
    sonar-01:
      ansible_host: ${clickhouse[0]["network_interface"][0]["nat_ip_address"]}
    nexus-01:
      ansible_host: ${clickhouse[1]["network_interface"][0]["nat_ip_address"]}
  children:
    sonarqube:
      hosts:
        sonar-01:
    nexus:
      hosts:
        nexus-01:
    postgres:
      hosts:
        sonar-01:
  vars:
    ansible_connection_type: paramiko
    ansible_user: ${username}
